const Title = ({ title }) => {
    return (
        <div className="col-lg-4 offset-lg-4">
            <div className="section-title text-center">
                <h1>{title}</h1>
            </div>
        </div>
    )
}

export default Title;